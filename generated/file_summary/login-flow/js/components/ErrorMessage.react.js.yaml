file_name: ErrorMessage.react.js
file_path: js/components/
file_type: JavaScript file

imported_libraries:
  - React
  - PropTypes
  - connect

component_name: ErrorMessage

component_props:
  - errorMessage

component_structure:
  - div with class "error-wrapper"
    - p with class "error" and text content from "props.errorMessage"
  - empty div if props.errorMessage is not truthy

component_mappings:
  - mapStateToProps maps state.errorMessage to props.errorMessage

default_export: ErrorMessage