
    Implement the test cases.
    Create tests in the test framework of choice.
    An example of the test framework is following

    describe('Test main page with login and check token & redirect', () => {
      it('it-login-success', () => {
        // Navigate to login page
        cy.visit('/login');

        // Enter valid email and password
        cy.get('#email').type('Email Input With Less Than 30 Letters');
        cy.get('#password').type('Password Input With Less Than 20 Letters Including At Least "!@#$%^&*" Characters');

        // Wait for 5 seconds
        cy.wait(5000);

        // Click login button
        cy.get('body > div > div > .button').click();

        // Check if the authorization header not empty
        cy.request({
          url: '/main',
          headers: { authorization: '' }
        }).its('headers.authorization').should('not.be.empty');

        // Check if the page redirected to the main page
        cy.url().should('include', '/main');
      });

     